# Popping 绿坦 / Ness 恩怨

导入粘贴用 **`导入用正文.txt`**（纯源码：`##` `###` `**` `-` `1.` `>>t` `>>`）。

不要用编辑器的 Markdown 预览复制。打开方式：右键 → Open With → Text Editor。

分类 **赛事动态**。配图在 `images/`，导入前拷到 `server/data/uploads/`，正文里的图片文件名不要改。

封面 `ness-greenteck-cover.png`。字段见 `meta.md`。
