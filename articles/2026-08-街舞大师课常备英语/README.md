# 街舞大师课常备英语

导入粘贴用 **`导入用正文.txt`**（纯源码：`##` `###` `**` `-` `>>t` `>>`）。

不要用编辑器的 Markdown 预览复制——预览里标题和加粗是渲染结果，粘进后台会丢格式符号。打开方式：右键文件 → Open With → Text Editor，或关掉 Markdown Preview。

`正文.md` 给人读；真正导入用 txt。小红书轮播贴 `小红书发布.md`。

分类 **文化历史**。暂无配图，封面后补。

字段见 `meta.md`。
