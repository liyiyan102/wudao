粘贴进 Excel「正文」列请用同目录 **导入用正文.txt**（纯源码，不会被预览成标题/加粗）。

不要从 Markdown 预览里复制。打开方式：右键 → Open With → Text Editor，或关掉 Markdown Preview。

语法与 `docs/内容导入模板.md` 一致：`>>t` / `>>` / `##` / `###` / `-` / `1.` / `**` / `> ——` / `![说明](文件名.jpg)`。
