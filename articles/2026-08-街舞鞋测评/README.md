# 街舞鞋测评

导入粘贴用 **`导入用正文.txt`**（纯源码：`##` `###` `**` `-` `1.` `![说明](文件名.jpg)`）。

不要用编辑器的 Markdown 预览复制——预览里标题和加粗是渲染结果，粘进后台会丢格式符号。打开方式：右键文件 → Open With → Text Editor，或关掉 Markdown Preview。

`正文.md` 给人读；`导入用正文.md` 与 txt 同内容。

分类 **穿搭**。配图在 `images/`，导入前拷到 `server/data/uploads/`，正文里的图片文件名不要改。

字段见 `meta.md`。
