<view class="page">
  <view class="lead">
    <view class="lead-title">使用微信登录</view>
    <view class="lead-sub">点头像选微信头像，点输入框选微信昵称</view>
  </view>

  <button class="avatar-btn" open-type="chooseAvatar" bindchooseavatar="onChooseAvatar">
    <image wx:if="{{avatar}}" class="avatar-img" src="{{avatar}}" mode="aspectFill" />
    <view wx:else class="avatar-ph">
      <view class="ic ic-profile"></view>
      <text>选头像</text>
    </view>
  </button>

  <view class="field">
    <text class="field-label">微信昵称</text>
    <input
      class="field-input"
      type="nickname"
      placeholder="点击选择微信昵称"
      value="{{nickname}}"
      maxlength="20"
      bindblur="onNickBlur"
      bindinput="onNickInput"
      bindchange="onNickChange"
      bindnicknamereview="onNickReview"
    />
  </view>

  <button class="submit" bindtap="onSubmit" loading="{{saving}}" disabled="{{saving}}">登录</button>
  <view class="hint">登录即表示同意
    <text class="link" bindtap="goAgreement">《用户协议》</text>
  </view>
</view>
